from typing import List

import game_message


# def allo(game_message: game_message.TeamGameState, action: List[game_message.Action]) -> List[game_message.Action]:
#     return True


# if move.type == "MOVE_TO":
#     move_to_position: Position = move.position
#     if util.is_not_in_enemies_zone(game_message.teamIds, game_message.currentTeamId, move_to_position,
#                                    game_message.teamZoneGrid) \
#             and util.is_not_in_enemies_zone(game_message.teamIds, game_message.currentTeamId, character.position,
#                                             game_message.teamZoneGrid):
#         self.current_state[character.id] = rcr.strategy_state.STACK_RESOURCES
# else:
#     character_actions.append(move)